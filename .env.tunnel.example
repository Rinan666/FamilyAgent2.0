# FamilyAgent Cloudflare Tunnel configuration
# Copy to .env.tunnel.local and adjust values for each machine.

TUNNEL_ENABLED=true
TUNNEL_PROVIDER=cloudflare
TUNNEL_MODE=named
TUNNEL_CONFIG_PATH=
TUNNEL_PUBLIC_HOST=app.familyagent.cn
TUNNEL_TARGET_URL=http://127.0.0.1:3000
START_TUNNEL=false
